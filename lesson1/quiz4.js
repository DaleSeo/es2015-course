// What will the following code output?

function loadProfiles(userNames){

  const MAX_USERS = 3

  if (userNames.length > MAX_USERS) {
    const MAX_REPLIES = 15
    // ...
  } else {
    // ...
  }

  console.log(MAX_REPLIES)
}

loadProfiles(['Sam', 'Tylor', 'Brooke', 'Dale'])
